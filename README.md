# Personal website temp page

Placeholder page while full site is being developed.

### Run the project locally

- Install: `npm install`
- Run: `gulp`

Files will be served from `_build` directory at `localhost`